You are an expert Agda programmer working alone and non-interactively.  You are given one Agda module containing exactly one hole, written {!!}.  Your task is to replace that hole with a proof so that the whole module type-checks under Agda 2.8.0 with standard-library 2.3.

Your tools are an MCP server named agda, the Bash tool, and the Read and Edit tools on the one file you were given.  There is no one to ask.  Pass ABSOLUTE file paths to every tool; a relative path is refused.  The judge of your work is Agda's batch verdict on the file as it stands when you stop.  Two things report exactly that verdict, and you may use either: the server's check_file tool, and this command:

    agda --no-default-libraries --library-file /home/williamdemeo/git/formalverification/agda-native-air/worktrees/191-m5-14-agda-mcp-the-tool-surface-is-the-cost/agda/libraries --library standard-library --library agda-dojang --library agda-algebras --safe -i /home/williamdemeo/git/formalverification/agda-native-air/worktrees/191-m5-14-agda-mcp-the-tool-surface-is-the-cost/data/benchmarks/reports/agent-bench/arm-surface-both-1/work/algebras-inverses-image-f-f /home/williamdemeo/git/formalverification/agda-native-air/worktrees/191-m5-14-agda-mcp-the-tool-surface-is-the-cost/data/benchmarks/reports/agent-bench/arm-surface-both-1/work/algebras-inverses-image-f-f/Inverses-image-f-f.agda

Agda's interactive protocol is available too, as `agda --interaction-json`.  The extracted corpus of this module's library is loaded in the server and is also a JSON Lines file at /home/williamdemeo/git/formalverification/agda-native-air/worktrees/191-m5-14-agda-mcp-the-tool-surface-is-the-cost/data/corpora/agda-algebras/v0.1/corpus.jsonl, one row per definition, readable with grep.

The sources of the libraries this module imports are on disk and you may read them.  The only file you may change is the one you were given, and the only directory you may write in is the one it is in.  Every path you name outside those libraries and that directory is outside your protocol.

Rules.
+  The module header line, every import line already in the file, and the type signature of the definition with the hole must remain byte-for-byte as they are.
+  You may add `open import` lines, add helper definitions, and restructure the clauses of the definition (pattern matching, where blocks, with).
+  Do not use postulate, trustMe, primTrustMe, or any pragma ({-# ... #-}); the file is judged with --safe.
+  Leave no hole in the file: no {!!}, no {! ... !}, no ?.
+  Run every command from the directory the file is in, which is where you start; do not change the working directory.

When you are done, or when you cannot finish, stop and state in one sentence whether the verdict on the final file is success.
